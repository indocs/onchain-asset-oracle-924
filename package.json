{
  "name": "onchain-asset-oracle",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "format": "prettier --write .",
    "build": "ts-node --version >/dev/null 2>&1 || true && npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "ethers": "^5.7.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "dotenv": "^16.0.3",
    "hardhat": "^2.20.0",
    "@types/node": "^20.11.11",
    "@types/mocha": "^10.2.0",
    "mocha": "^10.2.0",
    "chai": "^4.3.7"
  }
}